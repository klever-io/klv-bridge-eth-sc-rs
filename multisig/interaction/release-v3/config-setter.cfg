#### Ethereum

function set-eth-contracts-settings() {
  update-config RELAYER_ADDR_0 klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config RELAYER_ADDR_1 klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config RELAYER_ADDR_2 klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config QUORUM 3
  # Value in KFI and without decimals (e.g., 10 means 10 KFI)
  update-config RELAYER_REQUIRED_STAKE 10
  update-config SLASH_AMOUNT 0

  # Value in KFI and without decimals (e.g., 10 means 10 KFI)
  update-config ORACLE_REQUIRED_STAKE 10
  update-config AGGREGATOR_SLASH_AMOUNT 2
  update-config AGGREGATOR_SLASH_QUORUM 2
  update-config AGGREGATOR_SUBMISSION_COUNT 3

  manual-update-config-file
}

function set-eth-USDC-token-settings {
  update-config NR_DECIMALS_CHAIN_SPECIFIC 6
  update-config NR_DECIMALS_UNIVERSAL 6
  update-config NR_DECIMALS_ERC20 6
  update-config UNIVERSAL_TOKEN USDC-c76f1f
  update-config CHAIN_SPECIFIC_TOKEN_TICKER ETHUSDC
  update-config CHAIN_SPECIFIC_TOKEN ETHUSDC-220753
  update-config ERC20_TOKEN 0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48
  update-config MINTBURN_WHITELIST true
  update-config NATIVE_TOKEN false
  update-config FEE_AMOUNT 20
  update-config MAX_AMOUNT 50000
  update-config ETH_MAX_AMOUNT 1000000000000

  manual-update-config-file
}

### common

function set-oracle-addresses {
  update-config ORACLE_ADDR_0 klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config ORACLE_ADDR_1 klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config ORACLE_ADDR_2 klv1_REPLACE_WITH_ACTUAL_ADDRESS

  manual-update-config-file
}

function set-eth-contracts {
  update-config AGGREGATOR klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config SAFE klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config BRIDGED_TOKENS_WRAPPER klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config MULTI_TRANSFER klv1_REPLACE_WITH_ACTUAL_ADDRESS
  update-config MULTISIG klv1_REPLACE_WITH_ACTUAL_ADDRESS
}